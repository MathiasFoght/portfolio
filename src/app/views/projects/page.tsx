import type React from "react";

const Projects: React.FC = () => {
    return (
        <div id="projects" style={{height: "100vh", background: "black"}}>
            <h1>Projects</h1>
        </div>
    );
}

export default Projects;